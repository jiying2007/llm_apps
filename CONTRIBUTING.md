# Contributing

Use a branch and pull request. Do not push production changes directly to `main`.

Required before merge: native core host tests, Android CI, repository contract, and HarmonyOS build/device evidence for changes touching the Harmony shell or shared ABI. Release binaries and signing material belong in release infrastructure, not Git.
