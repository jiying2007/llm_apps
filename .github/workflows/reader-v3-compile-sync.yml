name: Reader V3 one-shot compile sync

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  sync:
    if: github.head_ref == 'feat/reader-v3-prelaunch-final'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          ref: feat/reader-v3-prelaunch-final
          fetch-depth: 0
      - name: Apply compile sync
        run: |
          set -euo pipefail
          python3 scripts/patch-reader-v3-compile.py
          if git diff --quiet; then
            echo 'Reader V3 compile sync already applied'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            apps/android/app/src/main/java/com/junchen/jingdu/ReaderScreenV3.kt \
            apps/android/app/src/main/java/com/junchen/jingdu/ReaderSettingsScreen.kt \
            apps/android/app/src/main/java/com/junchen/jingdu/ReaderV3Panels.kt
          git commit -m 'fix(reader): align V3 Compose compile APIs'
          git push origin HEAD:feat/reader-v3-prelaunch-final
